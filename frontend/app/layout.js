export const metadata = { title: 'BTCUSDT Futures AI Simulator' };

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, background: '#0b1220', color: '#d1d5db', fontFamily: 'Inter, Arial' }}>{children}</body>
    </html>
  );
}
